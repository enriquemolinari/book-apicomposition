# This can be generated by the build system to get values from a shared place
shows.server.host=http://localhost
shows.server.port=8092
shows.httpcall.timeout=2
# In Shows Config (movies info by Ids)
movies.ids.param.name=movieIds
shows.path.participate=/composed/shows
shows.path=/shows
# In User Profile Config (status and purchases)
shows.buyer.profile.path=/shows/buyer
shows.buyer.profile.path.participate=/composed/users/private/profile
shows.users.id.param.name=userId
# In Show Sales Config (sales info to send the email notification)
shows.sale.id.param.name=salesId
show.sale.participate=/composed/shows/sale
shows.sale.path=/shows/sale/%s